import re

palavras_proibidas = ["carlos", "templeuser"]
 removidos = []
 
 for group_name in list(groups.keys()):
     for palavra in palavras_proibidas:
         if re.search(palavra, group_name, re.IGNORECASE):
             removidos.append(group_name)
             del groups[group_name]
             break
